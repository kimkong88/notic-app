# SPA fallback: serve index.html for all routes so /pip and other client routes work (PiP iframe, deep links).
# Used by Netlify and Cloudflare Pages when this file is in the build output.
/*    /index.html   200
